services:
    web:
        build: .
        ports:
            - "3000:3000"
        environment:
            NODE_ENV: production
            RESEND_API_KEY: ${RESEND_API_KEY}
        env_file:
            - .env
